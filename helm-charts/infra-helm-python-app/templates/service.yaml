apiVersion: v1
kind: Service
metadata:
  name: {{ include "infra-helm-python-app.fullname" . }}
  labels:
    {{- include "infra-helm-python-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infra-helm-python-app.selectorLabels" . | nindent 4 }}
